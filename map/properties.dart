// Properties
// get all keys
// get all values
// Is Map empty
// Length

void main(List<String> args) {
  Map<String, num> numbers = {"a": 10, "b": 20, "c": 30, "d": 40, "e": 40};
  print(numbers);

  // get all keys
  // var allKeys = numbers.keys;
  // print(allKeys);
  // print(allKeys.runtimeType);
  // print(allKeys.toList());
  // print(allKeys.toSet());

  // get all values
  // var allValues = numbers.values;
  // print(allValues);
  // print(allValues.runtimeType);
  // print(allValues.toList());
  // print(allValues.toSet());

  // Is Map empty
  // print(numbers.isEmpty);
  // print(numbers.isNotEmpty);

  // Length
  // print(numbers.length);
}
