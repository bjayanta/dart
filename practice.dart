void main(List<String> args) {
  // String Concatenation
  // String first_name = "Jayanta";
  // String last_name = "Biswas";
  // String full_name = first_name + " " + last_name;

  // print(full_name);

  // Properties Of String
  // print(full_name.length);

  // String a = "A";
  // print(a.codeUnits);
  // print(full_name.codeUnits);

  // full_name = "";
  // print(full_name.isEmpty);
  // print(full_name.isNotEmpty);

  // Methods Of String
  // print(full_name.toLowerCase());
  // print(full_name.toUpperCase());

  String a = "     abc      ";
  print("The string contain $a .");
  print("The string contain ${a.trim()} .");
  print("The string contain ${a.trimLeft()} .");
  print("The string contain ${a.trimRight()} .");
}
