/**
 * Static:
 * 
 * To define a variable or method that is shared by all instances of a class.
 * Static members are accessed using the class name. 
 * Used for memory management.
 * Initialized only once when the class is loaded. 
 * Used to store the class-level data.
 */

/**
 * Static keyword used on:
 * 
 * Static Variable(s)
 * Static Method(s)
 */
