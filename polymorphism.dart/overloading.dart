/**
 * Dart doesn't have overloading as it is our experience that overloading leads to confusion. 
 * Especially when you intend to override a method but accidentally overload it. 
 * We think that optional arguments (named or not) are a better alternative.
 * 
 */
